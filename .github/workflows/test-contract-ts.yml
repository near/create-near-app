name: Test Contract TS
on:
  push:
    paths: ["templates/contracts/**/ts/**", "src/**", "index.js", "package.json"]
  pull_request:
    paths: ["templates/contracts/**/ts/**", "src/**", "index.js", "package.json"]
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true
env:
  NODE_ENV: 'ci'
jobs:
  tests:
    strategy:
      matrix:
        platform: [ubuntu-latest, macos-latest]
        node-version: [22, 24]
        template: [auction, auction-adv]
    runs-on: ${{ matrix.platform }}
    steps:
    - uses: actions/checkout@v4
    - uses: actions/setup-node@v4
      with:
        node-version: ${{ matrix.node-version }}
    - name: Install modules
      run: npm install
    - name: Create Contract TS - ${{ matrix.template }}
      run: npm run start -- test-app --frontend none --contract ts --template ${{ matrix.template }}
    - name: Install test app dependencies
      working-directory: test-app
      run: npm install
    - name: Run tests
      working-directory: test-app
      run: npm run test
